# 正式 H3 视频流程

此目录仅保留正式文件：

- n8n-正式主流程.json：线上 gene-video 主流程，已启用。
- n8n-正式视频片段.json：线上视频片段子流程，ID TlQWcAQoa4n72j4P，已替换 H3。
- comfyui-h3-workflow.json：ComfyUI 界面流程。
- comfyui-h3-api.json：n8n 所用 H3 API 节点图。

线上已更新，不需要再次导入。主流程传入 image_filepath、prompt、duration、width、height；seed 可选。A800 80GB 正式流程使用 `minimax_h3_fl2va_bf16.safetensors`；原 `minimax_h3_fl2va_pruned_int8_convrot.safetensors` 保留作回退。模型与采样配置固定，片段不带 H3 音轨，由原主流程添加旁白与音乐。单段时长 4–15 秒，尺寸舍入至 32 的倍数，帧数按 24fps、17k+5 对齐，因此实际时长可能略长。

最新正式默认（2026-09-14 15:20 北京时间）：BF16 DiT + `minimax_h3_fl2v_turbo_8step_v1.0_comfyui_bf16.safetensors`，LoRA 强度 1，video/audio shift 6/3，Euler + simple，8 steps；CLIP 和 VAE 不变。子流程可选输入 `generation_profile`：省略或 `turbo8` 使用快速模式，`quality20` 使用原 20 steps / res_multistep / 无 LoRA 模式。主流程当前未映射该可选参数，因此默认走快速模式；需要回退时在父流程执行子流程节点的输入映射中指定 `generation_profile=quality20`。原 builder 另备份在 `C:/Users/shana/.codex/backups/h3-20260914/builder-before-turbo.js`。

2026-09-14 同图、同提示词、864×480、124 帧、20 steps 实测：INT8 子流程约 451.5 秒；BF16 冷启动约 221.9 秒，热启动约 131.1 秒。正式子流程已切换 BF16，线上工作流 ID 仍为 `TlQWcAQoa4n72j4P`。

2026-09-14 音频迁移完成：配音（`🦅单人视频-生成配音`，IndexTTS2）、背景音乐（`🪿单人视频-生成背景音乐`，ACE-Step）、gene-music-full-song 整曲生成及其 Webhook 变体，ComfyUI 地址均由 A800 `117.50.214.226:8188` 改为 4090 `117.50.171.219:8188`（A800 镜像整体拷贝，认证 token 不变，n8n 凭据 `Bearer vincentzou` 沿用）。H3、分镜图片、人物生成仍在 A800。配音图谱 `unload_model` 已改为 `false`（IndexTTS2 在 4090 常驻）。

新机器实测（4090，IndexTTS2）：冷启动 103 秒；常驻模式首次 41 秒、稳态纯推理约 11 秒（对比 A800 上约 88 秒/次）。

2026-09-14 音频迁移后全流程实测（小羊图、864×480、5 秒、单片段）：首轮 7217 端到端 277 秒（H3 冷 202s + 配音 13s + 音乐首载 57s）；第二轮 7219 端到端 **186 秒**（H3 151s 含与首轮的排队等待、配音 17s、音乐 13s）。无排队干扰的稳态预估约 165 秒（2 分 45 秒）。对比迁移前 9 分 38 秒。

遗留：n8n 服务器存在 `COMFYUI_ADDR` 环境变量且指向已失效地址；两个 Webhook 变体的地址已改为写死 `http://117.50.171.219:8188`，不再依赖该变量。

回退备份移至 /Users/huizhonghe/.codex/backups/wellbeing-h3-20260914，不混入正式交付目录。

2026-09-14 后续独立端到端复测：主流程 `7234` 成功，耗时 536.125 秒；H3 子流程 `7235` 501.583 秒，配音约 15.5 秒，音乐约 13.5 秒，其余约 5.5 秒。输出：https://z.wellbeing.newstaredu.cn/wellbeing/media-cache/videos/2026/09/14/1789366451897-ks3io754pwn.mp4（HEAD 200，video/mp4）。实际配置确认 BF16 DiT、NVFP4 AWQ 文本编码器、864×480、124 帧、20 steps。

缓存核对：`7235` 的 ComfyUI `execution_cached.nodes` 为空，纯 ComfyUI 执行约 493.0 秒；先前 `7228` 则命中模型加载及 `MiniMaxH3ImageToVideo` 条件编码等 8 个节点，纯执行约 119.6 秒。约 2 分钟的热测包含条件编码缓存收益，不能作为新图片/新提示词的稳态保证；冷缓存额外开销约 373 秒，但现有 history 不能进一步拆分加载、编码和显存换入换出耗时。运行时无排队，显存余量曾降至约 2.5 GiB。

本轮未删除服务器模型、未升级 ComfyUI（探测仍为 0.30.0）。n8n HTTP 代理可运行/检测流程，但尚无 ComfyUI 宿主机文件操作、备份、环境升级和重启通道。不要将上述操作描述为已完成。

后续根因细化（ComfyUI `/internal/logs/raw`）：`7235` 条件编码/首帧 VAE 前约 62 秒，模型准备至进度条开始约 288 秒，20 步采样 108 秒，解码/保存约 31 秒。加载/初始化占大头，而不是采样步速变慢。仅凭这些日志仍不能区分磁盘冷页读取与权重换入的具体比例。

新提示词复测（prompt_id `c8f6da53-1c62-426c-af08-e90f540f483c`）：保持图像/尺寸/20 步不变，新增一句动作指令、更换 seed，`MiniMaxH3ImageToVideo` 未命中缓存；纯 ComfyUI 执行成功，121.97 秒。条件编码约 3 秒，采样约 108.5 秒，解码约 10.3 秒。因此新条件编码本身并非必然耗时数分钟，模型热态是关键。

预热工具：`scripts/h3-diagnostic.ps1 warmup` 通过 n8n 认证代理提交 256×160、5 帧、1 步的非业务预热任务，先检查 GPU 队列空闲，不释放其他模型、不改变正式参数。启动后需轮询返回的 prompt_id，直到成功，再放行业务请求。该脚本尚未接入宿主机启动过程，也未验证重启后的冷启动效果，不能当作已经解决冷启动的自动服务。`-Turbo` 为待安装的 8 步 FL2V v1.0 LoRA 提供预热（模型强度 1、video/audio shift 6/3）。

并行核对：2026-09-14 本次读取线上主流程 `aXjvMzbOugVM8uFm` 仍是 H3 → 配音 → 首次合成 → 背景音乐 → 最终合成；`7234` 对应启动时间 14:05:16、14:13:37、14:13:56（北京时间），没有重叠。暂未更改主流程，以免覆盖用户可能在其他版本上的并行改动。

Turbo 验证记录：冷缓存任务 `033adc29-e207-4234-8c11-e0ec6e28625c` 成功，纯生成 428.21 秒，其中 8 步采样约 40.9 秒；冷初始化仍很慢。独立 n8n 验证子流程 `7310` 成功，换提示词强制重算条件编码后，纯生成 57.06 秒、连下载落盘 60.83 秒。相同输入/seed 改用 res_multistep 的对照纯生成 53.56 秒（命中条件编码缓存），采样约 43 秒，未见明显背景遵循改善，正式选择与训练 Euler 更新对应的 Euler。输出可播放，864×480、124 帧、约 5.17 秒；观察到相对参考图新增背景和角色画面尺寸变化，不能承诺与原 20 步质量等同。

正式端到端 `7323` 成功（子流程 `7324`），2026-09-14 15:20:26–15:21:59 北京时间，共 92.781 秒：H3 60.632 秒、配音 13.528 秒、BGM 12.026 秒、其余约 6.595 秒。实际图谱确认 `turbo8`、8 steps、Euler、正确 LoRA。输出：https://z.wellbeing.newstaredu.cn/wellbeing/media-cache/videos/2026/09/14/1789370518809-n46vwzjfxd.mp4。配音在 H3 完成后启动，BGM 在第一次合成后启动，仍是串行；本轮未修改父流程结构。与原 577.7 秒总时长相比，本次热态总时长约缩短 84%，但不能将该比例推广至冷启动。

带 LoRA 的 1 步微型预热任务 `8dcef283-91a2-49fe-8249-da9c55dd744e` 用于验证预热脚本；模型已热，不能替代重启后冷预热验证。脚本默认从当前快速 API 图谱读取，`-Turbo` 保留兼容。预热应放在服务器启动/恢复后、业务放行前；当前没有宿主机启动配置通道，尚未自动接入。不要每条任务前调用 `/free`，否则会丢失模型热态收益。

本地验证：`node scripts/verify-h3-turbo.cjs` 检查默认快速/quality20 回退配置、非法 profile 拒绝、API 图谱和 ComfyUI 子图节点/连接一致性。独立测试工作流 `NjnHZDMngkTXW4gr` 已删除，生成的测试视频未删除。

2026-09-14 主流程并行改造已上线：主流程 `aXjvMzbOugVM8uFm` 在视频循环前异步启动原生音频协调子流程 `2QPcRKzRzhvlVKAM`（`waitForSubWorkflow=false`）。协调子流程在 4090 上顺序生成配音和 BGM，将结果写入现有工作流结果表，以 `parallel-audio:<主执行ID>` 关联；主流程在视频片段汇总后读取该结果，未完成则每 5 秒等待，超过 30 分钟或结果报错则失败，不会使用其他任务的音频。视频/音频跨 GPU 真并行；配音/BGM 同 GPU 仍顺序。原配音/BGM节点名保留，但现在只读取已经生成的结果，不再重复调用。合成文件名增加执行 ID 防止并发覆盖。

独立临时主流程执行 `7336` 成功，共 67.173 秒：H3 `7339` 于 15:52:19.618 开始，配音 `7338` 于 15:52:19.614 开始（相差 4ms）；BGM `7340` 于 15:52:34.756 开始，音频协调 `7337` 于 15:52:46.967 完成。H3 约 61 秒，音频约 27 秒，两者重叠。

正式入口执行 `7343` 成功，15:53:58.839–15:55:07.763，共 68.924 秒；H3 `7346` 60.677 秒，音频协调 `7344` 39.316 秒。相较此前串行热态 `7323` 的 92.781 秒，缩短约 26%。本次为模型热态，冷启动初始化问题仍未解决；1920×1080 是输入图尺寸，测试生成尺寸仍为 864×480，不代表原生 1080p 生成能达到相同时长。正式输出：https://z.wellbeing.newstaredu.cn/wellbeing/media-cache/videos/2026/09/14/1789372507268-jc3hptgs91.mp4。

主流程完整改造前备份：`C:/Users/shana/.codex/backups/h3-20260914/main-before-parallel.json`。恢复时用备份中的 nodes/connections 更新主流程；协调子流程须等所有在途任务结束再停用。本地检查 `node scripts/verify-h3-parallel.cjs` 覆盖异步配置、连接、任务关联、等待、成功、失败和超时分支。

正式复测 `7347` 与 `7343` 相隔约 25 秒提交，成功耗时 97.157 秒（存在同一 A800 上的视频排队，不能作为无排队耗时）；其音频严格关联 `parallel-audio:7347` / 协调执行 `7348`，未混用 `7343` 的 `7344`。结果已上传。独立临时主流程 `jPkdDHqpH3WW5xoO` 完成后已删除，测试视频与执行记录保留。
