{
  "name": "单环节重新生成 [v2]",
  "nodes": [
    {
      "parameters": {
        "model": {
          "__rl": true,
          "mode": "list",
          "value": "deepseek-v4-flash"
        },
        "responsesApiEnabled": false,
        "options": {
          "maxTokens": 4096
        }
      },
      "type": "@n8n/n8n-nodes-langchain.lmChatOpenAi",
      "typeVersion": 1.3,
      "position": [
        1424,
        272
      ],
      "id": "d4392581-fb0b-4a6d-a141-f79716a4a546",
      "name": "OpenAI Chat Model",
      "credentials": {
        "openAiApi": {
          "id": "EPHUtiZYNuqoGVK2",
          "name": "deepseek"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "allIncomingItems",
        "options": {}
      },
      "id": "2d1e7903-06ac-4048-b9d3-ca0663347052",
      "name": "返回步骤JSON",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        2192,
        64
      ]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "course-step-regenerator-v2",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "e1243544-c2db-46be-a43d-a0eae84c8f80",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        1072,
        64
      ],
      "webhookId": "course-step-regen-01-v2"
    },
    {
      "parameters": {
        "jsCode": "const input = $input.item.json.body;\nconst language = input.language || 'zh';\nconst outputLanguage = input.outputLanguage || (language === 'en' ? 'English' : 'Chinese');\n\nconst phaseInfo = {\n  engage: { label: 'Engage', description: '建立情感连接与情境导入，激发好奇心和参与动机', timeRatio: '15%' },\n  empower: { label: 'Empower', description: '输入语言知识与SEL技能，进行体感探索', timeRatio: '40%' },\n  execute: { label: 'Execute', description: '通过艺术表达完成终极产出任务，体验合作分享', timeRatio: '30%' },\n  elevate: { label: 'Elevate', description: '反思、内化并建立积极情绪闭环', timeRatio: '15%' }\n};\n\nconst phaseKey = input.phaseKey || 'engage';\nconst info = phaseInfo[phaseKey];\n\nconst phaseInfoEn = {\n  engage: { label: 'Engage', description: 'Build emotional connection, introduce the context, and spark curiosity and motivation' },\n  empower: { label: 'Empower', description: 'Introduce language knowledge and SEL skills through embodied exploration' },\n  execute: { label: 'Execute', description: 'Complete the final output task through creative expression and collaborative sharing' },\n  elevate: { label: 'Elevate', description: 'Reflect, internalize learning, and close the positive emotion loop' }\n};\nif (language === 'en' && phaseInfoEn[phaseKey]) {\n  info.label = phaseInfoEn[phaseKey].label;\n  info.description = phaseInfoEn[phaseKey].description;\n}\n\nconst parseDurationMinutes = (value) => {\n  const numbers = String(value || '').match(/\\d+(?:\\.\\d+)?/g);\n  if (!numbers || numbers.length === 0) return 0;\n  return Number(numbers[numbers.length - 1]) || 0;\n};\nconst formatMinutes = (value) => Number.isInteger(value) ? String(value) : value.toFixed(1).replace(/\\.0$/, '');\nconst ratioValue = Number(String(info.timeRatio || '').replace('%', '')) / 100;\nconst phaseRecommendedMinutes = parseDurationMinutes(input.duration || '60分钟') * (Number.isFinite(ratioValue) ? ratioValue : 0);\nconst siblingStepMinutes = Array.isArray(input.siblingSteps)\n  ? input.siblingSteps.reduce((sum, step) => sum + parseDurationMinutes(step.time || step.duration), 0)\n  : 0;\nconst replacementMaxMinutes = Math.max(0, phaseRecommendedMinutes - siblingStepMinutes);\n\nlet currentStepStr = '无';\nif (input.currentStep) {\n  const s = input.currentStep;\n  currentStepStr = `ID: ${s.id}\\n标题: ${s.title}\\n时长: ${s.time || s.duration || '未设置时长'}\\n目标: ${s.objective || ''}`;\n}\n\nlet siblingStepsStr = '无';\nif (input.siblingSteps && input.siblingSteps.length > 0) {\n  siblingStepsStr = input.siblingSteps.map((s, i) => `${i+1}. ${s.title} (${s.time || s.duration || '未设置时长'})`).join('\\n');\n}\n\nlet otherPhasesStr = '无';\nif (input.otherPhases) {\n  try {\n    otherPhasesStr = typeof input.otherPhases === 'string' ? input.otherPhases : JSON.stringify(input.otherPhases, null, 2);\n  } catch (e) {\n    otherPhasesStr = '无法解析';\n  }\n}\n\nconst __out = {\n  language,\n  outputLanguage,\n  age: input.age || '7-9岁',\n  duration: input.duration || '60分钟',\n  scale: input.scale || '≤ 8人',\n  title: input.title || '未命名课件',\n  vocabulary: (input.vocabulary && input.vocabulary.length > 0) \n    ? (Array.isArray(input.vocabulary) ? input.vocabulary.join(', ') : input.vocabulary)\n    : '请根据主题推荐词汇',\n  grammar: (input.grammar && input.grammar.length > 0) \n    ? (Array.isArray(input.grammar) ? input.grammar.join(', ') : input.grammar)\n    : '请根据主题推荐句型',\n  theme: input.theme || '默认主题',\n  requirements: input.requirements || input.userIdea || '',\n  classicName: input.classicName || '',\n  phaseKey: phaseKey,\n  phaseLabel: info.label,\n  phaseDescription: info.description,\n  timeRatio: info.timeRatio,\n  phaseRecommendedMinutes: formatMinutes(phaseRecommendedMinutes),\n  siblingStepMinutes: formatMinutes(siblingStepMinutes),\n  replacementMaxMinutes: formatMinutes(replacementMaxMinutes),\n  stepId: input.stepId || '',\n  currentStep: currentStepStr,\n  siblingSteps: siblingStepsStr,\n  otherPhases: otherPhasesStr\n};\n// ─── 提示词注册表渲染（模板由后端下发：api/src/prompts，Wiki 页面可覆盖）───\nconst __renderPrompt = (tpl, vars) => String(tpl).replace(/\\{\\{\\s*(\\w+)\\s*\\}\\}/g, (m, k) => (vars[k] === undefined || vars[k] === null) ? '' : String(vars[k]));\nconst __template = input.promptTemplate;\nif (!__template) throw new Error('缺少 promptTemplate：后端提示词注册表未下发，请先部署最新 api/src/prompts 再导入本工作流');\n__out.system_prompt = __renderPrompt(__template, __out);\nreturn __out;\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1280,
        64
      ],
      "id": "83b41ebc-c68b-4b08-a8c3-c0a010ce77d2",
      "name": "参数组装"
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "={{ $json.system_prompt }}",
        "batching": {}
      },
      "type": "@n8n/n8n-nodes-langchain.chainLlm",
      "typeVersion": 1.9,
      "position": [
        1488,
        64
      ],
      "id": "382939ac-f062-42a4-affd-73ff8d36db55",
      "name": "Basic LLM Chain"
    },
    {
      "parameters": {
        "jsCode": "// 解析 LLM 输出的 JSON 文本\nlet raw = $input.item.json;\n\n// 如果 LLM 输出有 text 字段，从中提取 JSON\nif (raw.text && typeof raw.text === 'string') {\n  const match = raw.text.match(/\\{[\\s\\S]*\\}/);\n  if (match) {\n    try { raw = JSON.parse(match[0]); } catch (e) {}\n  }\n}\n\nconst step = (raw.step && typeof raw.step === 'object') ? raw.step : raw;\n\n// 补充 activitySteps：按内容动态拆分，避免固定四步模板\nif (!step.activitySteps || step.activitySteps.trim() === '') {\n  const activity = step.activity || '';\n  const sentences = activity.split(/[。！？；;]+/).map(s => s.trim()).filter(Boolean);\n  const makeStepName = (text, index) => {\n    const cleaned = String(text || '').replace(/[“”\\\"'，、。！？；;：:（）()]/g, ' ').trim();\n    const firstWords = cleaned.split(/\\s+/).filter(Boolean).slice(0, 2).join('');\n    if (firstWords) return firstWords.slice(0, 8);\n    return `课堂推进${index + 1}`;\n  };\n  const targetCount = Math.max(2, Math.min(6, sentences.length || 3));\n  const size = Math.max(1, Math.ceil((sentences.length || 1) / targetCount));\n  const parts = [];\n  for (let i = 0; i < targetCount; i++) {\n    const chunk = sentences.slice(i * size, (i + 1) * size);\n    const text = chunk.length > 0 ? chunk.join('。') + '。' : '教师根据课堂进展自然推进活动。';\n    parts.push(makeStepName(text, i) + '：' + text);\n  }\n  step.activitySteps = parts.join('\\n');\n}\n\n// 补充 resources（从 assets[].title 提取）\nif (!step.resources || step.resources.trim() === '') {\n  const assets = Array.isArray(step.assets) ? step.assets : [];\n  step.resources = assets.map(a => a.title || '').filter(Boolean).join('、');\n}\n\n// 补充 scenario（从 activity 第一句截取）\nif (!step.scenario || step.scenario.trim() === '') {\n  const activity = step.activity || '';\n  const first = activity.split(/[。！？]/)[0] || '';\n  step.scenario = first.length > 60 ? first.slice(0, 60).replace(/[，、]$/, '') : first;\n}\n\nreturn { step: step };\n"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1984,
        64
      ],
      "id": "52c9c133-0e8c-4c58-b265-e7c055c96d41",
      "name": "补充缺失字段"
    }
  ],
  "pinData": {
    "Webhook": [
      {
        "json": {
          "headers": {
            "host": "117.50.218.161:5678",
            "connection": "keep-alive",
            "content-type": "application/json"
          },
          "params": {},
          "query": {},
          "body": {
            "phaseKey": "empower",
            "stepId": "em2",
            "title": "森林派对",
            "age": "7-9岁",
            "duration": "60分钟",
            "scale": "9-15人",
            "vocabulary": [
              "apple",
              "orange"
            ],
            "grammar": [
              "Can I have your apple/orange, please?"
            ],
            "theme": "森林探险",
            "currentStep": {
              "id": "em2",
              "title": "水果感知体验",
              "time": "10-15分钟"
            },
            "siblingSteps": [
              {
                "title": "神秘邀请",
                "time": "5-10分钟"
              }
            ],
            "otherPhases": {}
          },
          "webhookUrl": "http://117.50.218.161:5678/webhook/course-step-regenerator",
          "executionMode": "production"
        },
        "pairedItem": {
          "item": 0
        }
      }
    ]
  },
  "connections": {
    "OpenAI Chat Model": {
      "ai_languageModel": [
        [
          {
            "node": "Basic LLM Chain",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    },
    "Webhook": {
      "main": [
        [
          {
            "node": "参数组装",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "参数组装": {
      "main": [
        [
          {
            "node": "Basic LLM Chain",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Basic LLM Chain": {
      "main": [
        [
          {
            "node": "补充缺失字段",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "补充缺失字段": {
      "main": [
        [
          {
            "node": "返回步骤JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  },
  "versionId": "520415e4-cb35-4a87-adae-2730e17325da",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "7185505d6331a771c013a5da6d967be5270273cfc524175c4267715e3ff62d5c"
  },
  "id": "LWHWfOpAfpyKujIV",
  "tags": []
}
