# 交接文档：创作工坊三模块工作室化改造（2026-09-07）

> **后续**：09-08 的迭代（授课新窗口/游戏视觉修复/七步流程/国际化）见《交接-2026-09-08-创作工坊迭代.md》，本文遗留项的完成情况以该文为准。

> 目标读者：明天换机器继续开发的自己。所有改动**尚未提交**，换机器前务必 commit + push。

## 一、本次完成的三大目标

### 1. 互动式情境瑜伽（interactive-yoga）——从一表单一键生成升级为 4 步工作室
- 流程：基本信息 → 活动方案（AI）→ 页面设计（AI + 体式下拉 + 画面描述）→ 制作插图（批量/逐页生成与重绘）。
- 插图链路复用绘本 B9（`/api/ai/generate-ppt-asset` → n8n `ppt-storybook-generator` → ComfyUI → OSS）。动作页插图会清楚示范所选体式的身体动作；**图上不带任何文字**（体式名标注功能已按需求移除）。
- 授课模式 = 应用内全屏播放（复用绘本 `pbv2-presentation`，←/→ 翻页、Esc 退出）；列表点卡片进编辑器（不再新开 HTML）；HTML 打开/下载按钮带 `?token=`（修复了新窗口 401 的隐藏 bug）。
- 新页面 `src/modules/creative-workshop/YogaStudioPage.jsx`；体式库常量 `api/src/lib/experience/yogaPoses.ts` + 前端副本 `yogaPoses.js`（55 体式，两处需同步）。

### 2. 星光录音棚（music-star-quest）——同样 4 步工作室，最终产出四关闯关游戏课件
- 流程：基本信息 → 歌曲创作（歌名+16 行时间轴歌词+目标句型，可逐行编辑）→ 练习与教案（选词填空/连词成句/听音选词按 4:3:2 题量规则 + 四关教案，全部可编辑）→ 音频与课件（上传原唱/伴奏 mp3 ≤8MB 转 base64 嵌入 → 生成课件 → iframe 全屏试玩）。
- 新页面 `src/modules/creative-workshop/MusicStudioPage.jsx`；游戏模板 `api/public/templates/music-star-quest.html`（由 newpage1 示例制作，剥离示例音频，注入点：lyrics/ex1FillData/ex2Items/ex3Data/teachingPlans/双音频 src）。
- `ExperiencePage.jsx` 已删除，绘本/瑜伽/音乐三模块均为独立工作室。

### 3. 规范文档维护进 Wiki.js 并在生成时真实调用
- 瑜伽：`wellbeing/docs/yoga-lesson-plan-spec`（教案规范）、`yoga-content-spec`（内容规范）、`yoga-pose-library`（体式匹配库）。
- 音乐：`wellbeing/docs/music-flow-spec`（流程与功能模块）、`music-level1-spec`（第一关题型设计规范）。
- 提示词页（可被教研覆盖、生成 5 分钟缓存生效）：`wellbeing/prompts/experience-yoga-plan`、`experience-yoga-design`（已更新为新版）、`experience-music-song`（重构）、`experience-music-exercises`（新增）。
- 注入机制：`api/src/lib/prompt-library.ts` 新增 `loadWikiPage(path)`；`generator.ts` 的 `withSpecDocs`/`withMusicSpecDocs` 在生成时把文档全文追加进 system 提示词，失败自动跳过。**改 Wiki 文档 5 分钟后生成生效，无需发版**。

## 二、主要改动文件

后端（api/）：
- `src/lib/experience/generator.ts` — 核心：renderYogaHtml / generateYogaPlan / generateYogaDesign / generateMusicSong / generateMusicExercises / renderMusicGameHtml / 旧 generateMusic 改走新链路
- `src/lib/experience/yogaPoses.ts`（新）、`src/lib/prompt-library.ts`（loadWikiPage）
- `src/prompts/experience.ts`（yoga-plan/yoga-design/music-song/music-exercises 四组提示词）、`builtin.ts`、`registry.ts`
- 新路由 `src/app/api/creative-works/[id]/`：`plan/`、`design/`、`song/`、`exercises/`、`render/`；`route.ts` PUT 扩展（瑜伽重渲染 + 音乐分段保存）；`html/route.ts` 支持 `?token=` 查询参数鉴权
- `public/templates/interactive-yoga.html`（授课主图 pageImg 兜底）、`public/templates/music-star-quest.html`（新游戏模板）

前端（src/）：
- `modules/creative-workshop/YogaStudioPage.jsx`、`MusicStudioPage.jsx`（新）、`ExperiencePage.jsx`（删）、`workshopStorage.js`（update/plan/design/song/exercises/render）、`creativeWorkshop.css`、`yogaPoses.js`（新）
- `utils/assetGeneration.js`（新，resolveGeneratedAsset 从绘本抽出共用）、`modules/picture-book/PictureBookStudioPage.jsx`（改用共享工具）、`App.jsx`（路由）

## 三、换机器必读（环境）

1. **数据库走 SSH 双跳隧道**（本机 5432 → 测试机 118.31.36.124 的 Postgres）：
   `ssh -N -J root@8.130.93.151 -L 5432:127.0.0.1:5432 newstar@118.31.36.124`
   `api/.env` 的 `DB_PORT` 已改为 **5432**（原来是 15432；.env 不进 git，新机器要对齐）。
2. 本地服务：前端 vite（5174，/api 代理到 4000）、后端 `api` 目录 `next dev -p 4000`（热更新，改后端不用重启）。
3. 生图链路依赖 n8n（117.50.218.161:5678）+ ComfyUI GPU 容器；LLM 用 DashScope qwen-plus（api/.env 的 VITE_DASHSCOPE_API_KEY）；提示词/规范在 Wiki.js（118.31.36.124:3000）。
4. Node 版本：根目录 vite 要求 Node 20.19+（本机 20.18.3 只能跑 dev，build 时若报版本错需升级或忽略——本会话 build 实际成功过）。

## 四、遗留问题 / 明天待办

1. **瑜伽/音乐插图的出图验证被 GPU 服务阻塞**：2026-09-07 已进一步确认 n8n `/healthz` 返回 200，但工作流配置的 5 个 ComfyUI 端点（*.container.x-gpu.com:8188~8192）访问 `/system_stats` 均返回 503。根因在 GPU 实例侧，不是三个工作室共用的前后端调用代码；**绘本模块现在同样出不了图**。需在 GPU 云平台恢复/重启对应实例，再重新验证 `ppt-storybook-generator`。修好后瑜伽第 4 步批量/逐页出图即可用（链路与绘本完全同一条）。
2. ~~生成时听音选词（ex3）的音频片段目前固定从歌曲 25 秒处播 8 秒~~ **已完成（2026-09-07）**：ex3 题目新增 `time` 字段（对应歌词行时间段），提示词要求 LLM 原样复制歌词时间轴；游戏模板每题带独立播放按钮 `st1PlayClip(idx)` 按时间段取段播放（段长上限 12s，留空回退旧的 25s/8s）；前端 ListenEditor 每题可编辑时间段。
3. 体式库两份副本（api/yogaPoses.ts 与 src/yogaPoses.js）需要人工同步；若体式库在 Wiki 改了，生成会用新库但前端下拉不会变。**已核对一致（各 55 条），并新增校验脚本 `api/scripts/verify-yoga-pose-sync.mjs`（不一致时退出码 1，可在提交前跑一次）。**
4. AI 生成音乐音频（Suno 类）未接，接口已留好（第 4 步上传 mp3 是当前方案）。
5. 数据库连接是生产库（正式库在测试机上），测试时用临时用户并记得清理。

## 五、接手后的本地收尾（2026-09-07）

- 前端 `vite build` 与后端 `next build` 均通过（本机 Node 20.18.3 仍会显示 Vite 版本警告）。
- 体式同步校验脚本已改为兼容 Node 20+，实测两端 55 条一致。
- 修正 ESLint 对构建目录的误扫描；本次创作工坊相关文件定向 Lint 通过。全仓库仍有大量历史 Lint 错误，未在本次功能范围内批量改动。
- 删除误提交的明文数据库凭据临时脚本、空 Wiki 临时文件及 TypeScript 构建缓存，并补充 `.gitignore` 防止再次提交。
