# 企业级 Wiki 知识库与业务 API 落地实施指南

本指南梳理了基于 **Wiki.js + 本地 PostgreSQL 16 + 业务 API 对接** 的轻量级、高扩展企业知识库搭建全流程，包含已完成的操作记录与后续实施步骤。

---

## 一、 架构选型核心共识

* **前台知识维护：** 采用 **Wiki.js**，提供类似 Notion/飞书的现代化可视化界面（支持 Markdown 和富文本），适合团队日常协同录入。
* **数据存储与对接：** 复用服务器现有的 **PostgreSQL 16** 数据库，并通过原生 **GraphQL/REST API** 无缝对接下游业务系统。
* **RAG 扩展预留：** 后期可通过配置 **Webhook** 或 **Git 存储双向同步**，无缝衔接大模型向量化（Vector RAG）管道。

---

## 二、 已完成的操作与问题排查记录

### 1. 本地 PostgreSQL 16 数据库与权限初始化

由于 PostgreSQL 15/16 对 `public` schema 的安全策略调整，需手动赋予用户建表权限。

* **登录数据库并创建用户与库：**
```bash
sudo -u postgres psql

```


```sql
CREATE USER wiki_user WITH PASSWORD 'SecretPassword123!';
CREATE DATABASE wiki_db OWNER wiki_user;
GRANT ALL PRIVILEGES ON DATABASE wiki_db TO wiki_user;
\q

```


* **解决 `permission denied for schema public` 报错：**
由于 Wiki.js 初始化时需要在 `public` 空间创建 `migrations` 等表，必须修正 schema 所有权：
```bash
sudo -u postgres psql -d wiki_db

```


```sql
ALTER SCHEMA public OWNER TO wiki_user;
GRANT ALL ON SCHEMA public TO wiki_user;
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO wiki_user;
\q

```



### 2. 编写 `docker-compose.yml` 配置文件

在工作目录（如 `/home/newstar/llmwiki/`）下创建 `docker-compose.yml`，让 Wiki 容器直接挂载复用宿主机的 PostgreSQL：

```yaml
services:
  wiki:
    image: ghcr.io/requarks/wiki:2
    container_name: wiki-main
    restart: always
    ports:
      - "3000:3000"
    environment:
      DB_TYPE: postgres
      DB_HOST: 172.17.0.1
      DB_PORT: 5432
      DB_USER: wiki_user
      DB_PASS: SecretPassword123!
      DB_NAME: wiki_db

```

*(注：`DB_HOST` 使用 Docker 默认网桥网关 `172.17.0.1`，确保容器能顺畅连通宿主机数据库。)*

### 3. 启动容器与服务验证

* **启动容器：**
```bash
sudo docker compose up -d

```


* **检查容器运行状态：**
```bash
sudo docker ps

```


* **验证内部响应：**
```bash
curl -I http://127.0.0.1:3000

```


*(当返回 `HTTP/1.1 200 OK` 且包含 `X-Powered-By: Wiki.js` 即代表服务完全正常。)*

### 4. 远程安全访问（无公网 3000 端口时的方案）

在本地电脑通过 SSH 隧道将服务器的 3000 端口映射到本地：

```bash
ssh -L 3000:localhost:3000 newstar@<你的服务器公网IP>

```

随后在本地浏览器访问 `http://localhost:3000` 即可。

---

## 三、 后续需要进行的标准操作指南

### 1. 初始化 Web 端系统

1. 打开浏览器访问 `http://localhost:3000`（通过 SSH 隧道）。
2. 输入 **Administrator Email** 和 **Password** 创建超级管理员账号。
3. 填写 **Site URL**（后续可修改），点击 **INSTALL** 完成初始化。

### 2. 规划目录与权限控制（Page Rules）

1. 进入 **Administration -> Groups**，创建不同部门的角色（如 `HR-Team`、`Tech-Team`）。
2. 进入 **Page Rules** 配置路径隔离策略（例如设定规则 `hr/*` 仅允许 `HR Team` 访问），保障公司文档的机密性。

### 3. 开通业务系统 API 访问凭证

1. 进入 **Administration -> API Access**。
2. 创建 **Personal Access Tokens** 或系统用户令牌。
3. 下游业务系统通过 `http://<服务器IP>:3000/graphql` 发送请求，携带 `Authorization: Bearer <TOKEN>` 即可实时读写 Wiki 里的 Markdown 文档。

*(示例：业务系统按路径读取 Markdown 正文)*

```graphql
query {
  pages {
    singleByPath(path: "tech/backend/spec", locale: "zh") {
      id
      title
      content # 返回纯 Markdown 源码
    }
  }
}

```

### 4. 预留：对接 RAG/LLM 知识库

* **方案 A：** 在 **Administration -> Webhooks** 中配置事件监听（`Page Updated` / `Page Created`），当文档在界面更新时自动推送给后台的向量化服务。
* **方案 B：** 在 **Administration -> Storage -> Git** 中绑定内部 Git 仓库，实现所有 `.md` 文件的双向同步，供后台 Python 脚本直接做切片和向量入库。



/wellbeing/
├── /overview               (Wellbeing 项目总览与介绍)
├── /common/                (通用知识库)
│   ├── /design-assets      (视觉风格/设计规范 Design Tokens)
│   ├── /character-setting  (IP/角色形象设定与故事背景)
│   └── /pedagogy-standard  (儿童/健康教育理念与标准)
├── /picture-book/          (绘本制作)
│   ├── /sop                (绘本创作标准流程)
│   └── /scripts            (绘本脚本与分镜库)
├── /music/                 (歌曲编排)
│   ├── /composition-guide  (编曲/作词规范)
│   └── /audio-assets       (音效与配乐资源库)
├── /courseware/            (课件制作)
│   ├── /template           (交互课件/PPT 标准模板)
│   └── /interactivity-spec (课件交互与动画逻辑)
├── /yoga/                  (瑜伽动画制作)
│   ├── /action-standard    (瑜伽动作拆解与动作捕捉规范)
│   └── /animation-pipeline (3D/2D 动画制作流水线)
└── /games/                 (小游戏制作)
    ├── /game-design-doc    (游戏策划案 GDD)
    └── /logic-spec         (游戏逻辑与数值说明)

---

## 四、 业务对接 · Git 存储双向同步（已选定方案）

> 目标：`docs/markdown/` 下的 `.md` 源文件与 Wiki.js 双向同步——本地推送即更新 Wiki，Wiki 界面编辑也会提交回 Git；后续 RAG 直接消费 Git 仓库，无需再走 API 拉取。

### 架构

```
docs/markdown/*.md（本地 / CI）
        │  push（docs/wiki-sync/sync-to-wiki.sh 镜像同步）
        ▼
服务器裸仓库 /home/newstar/wiki-content.git
        ▲▼  Wiki.js Storage -> Git 模块（SSH 部署密钥，双向同步）
Wiki.js（wiki_db，页面路径 = 文件路径）
```

**为什么用独立裸仓库而不是直接绑项目仓库**：Wiki.js 会把仓库内所有 `.md` 导入为页面，wellbeing 项目里还有 `api/src/md/**`、`doc/**` 等无关 md，因此内容仓库只镜像 `docs/markdown/`。

### 步骤 A：服务器初始化（执行 `docs/wiki-sync/setup-server.sh` 或手动）

```bash
ssh newstar@<服务器IP>

git init --bare -b main ~/wiki-content.git

# 为 Wiki.js 容器生成部署密钥（公钥加入 authorized_keys）
ssh-keygen -t ed25519 -f ~/wiki_deploy_key -N "" -C "wikijs-deploy"
cat ~/wiki_deploy_key.pub >> ~/.ssh/authorized_keys

# 获取 SSH Host Key（取输出第 2、3 列，稍后填入 Wiki.js）
ssh-keyscan -t ed25519 localhost
```

### 步骤 B：本地推送（Git Bash / Linux / CI 通用）

```bash
cd d:/workspace/wellbeing
./docs/wiki-sync/sync-to-wiki.sh ssh://newstar@<服务器IP>/home/newstar/wiki-content.git
```

脚本行为：临时克隆内容仓库 → 镜像拷贝 `docs/markdown`（新增/修改/删除均同步）→ 无变化不提交 → 推送 `main`。

> Windows 下若脚本报 `$'\r'` 错误（换行符问题），先执行 `sed -i 's/\r$//' docs/wiki-sync/*.sh`。

### 步骤 C：Wiki.js 配置 Storage -> Git

1. 站点默认语言设为中文：**Administration → General → Locale**（保证文件与路径一一对应，不产生语言后缀文件）。
2. 确认现有 14 个空页面是用默认语言创建的；不一致的空页先删除，避免导入重复。
3. **Administration → Storage → Git → Set Active**：

| 配置项 | 值 |
|--------|-----|
| Repository URL | `ssh://newstar@<服务器IP>/home/newstar/wiki-content.git` |
| Branch | `main` |
| Auth | SSH Private Key（粘贴 `wiki_deploy_key` 私钥全文） |
| SSH Host Key | `ssh-keyscan` 输出的 key 部分 |
| Default Author Email | `wiki-bot@wellbeing.local` |

4. 保存后点 **Sync Now**，14 篇文档按路径导入。

### 同步规则与注意

| 事项 | 说明 |
|------|------|
| 路径映射 | `wellbeing/picture-book/sop` ⇄ `docs/markdown/wellbeing/picture-book/sop.md` |
| 同步频率 | Wiki.js 周期自动拉取（约 5 分钟），可手动 Sync Now 立即同步 |
| 单一写入源 | 同一页面避免本地与 Wiki UI 同时修改；策略：规范文档在 Wiki 改，AI/脚本产出推 Git |
| 页面标题 | 首次导入后建议在 UI 批量核对标题（每篇首行已是 `# 标题`，正文不受影响） |
| 安全 | 3000 端口保持内网/SSH 隧道；部署密钥仅授权该账号的仓库 |

### 验证清单

- [ ] `sync-to-wiki.sh` 推送成功，裸仓库可见 14 个 md
- [ ] Wiki.js Sync Now 后 14 个页面内容更新
- [ ] 在 Wiki UI 修改任一页 → 数分钟后裸仓库出现新 commit（反向同步验证）
- [ ] 本地删除一个测试文件推送 → Wiki 对应页面同步变化

### 后续扩展（本期不做）

* CI 定时执行 `sync-to-wiki.sh`（内网 Runner 或 self-hosted Actions）
* Webhook（Page Updated）→ 业务 API → 切片（700 字/100 重叠）→ Embedding → Qdrant，复用本内容仓库作为 RAG 数据源

---

## 五、 RAGFlow 接入与用户使用习惯沉淀（已实施）

> RAGFlow 接管绘本 RAG 检索链路（替换原 PG category 匹配的简化实现），并通过埋点 + ETL 将用户使用习惯沉淀为可检索画像，供 AI 生成个性化参考。

### 1. 总体架构

```
┌─ 采集 ─────────────────────────────────────────────────┐
│ 前端 usageTracker.js → POST /api/events → audit_logs   │
│ n8n 统一入口 call() 埋点 ai.workflow.call/error         │
│ upload-knowledge 埋点 rag.knowledge.upload             │
└──────────────────────┬─────────────────────────────────┘
                       ▼ 每日 cron
        api/scripts/sync-usage-habits.mjs
        （audit_logs + prompt_history + generation_tasks 聚合）
                       ▼ 组织/用户两级画像 Markdown
              RAGFlow 数据集 usage-habits
                       │
┌─ 消费 ───────────────┴─────────────────────────────────┐
│ /api/rag/generate → RAGFlow /v1/retrieval 向量检索       │
│   ├─ picturebook-knowledge 数据集（上传知识自动同步）     │
│   └─ usage-habits 数据集（个性化上下文）                 │
│ 检索失败/未启用 → 自动回落原 PG category 匹配             │
└────────────────────────────────────────────────────────┘
```

### 2. 部署 RAGFlow（deploy/ragflow/）

```bash
cd deploy/ragflow
docker compose up -d        # es01 + mysql + minio + redis + ragflow，需 >= 4C8G
```

* 访问 `http://<服务器IP>:9380` 注册管理员
* 系统模型设置中配置 LLM（推荐 DashScope/OpenAI 兼容端点）与 Embedding 模型
* 用户设置 → API → 生成 API Key
* 端口占用注意：9380/9200/5455/9000/9001/6379，与现有 5433/4000/5174/3000 不冲突；宿主已有 Redis 时改 6379 映射并同步 `service_conf.yaml`

### 3. 业务系统改动清单

| 文件 | 改动 |
|------|------|
| `api/src/lib/ragflow/client.ts` | 新增：RAGFlow REST 客户端（dataset/文档上传解析/检索） |
| `api/src/lib/usage-tracking.ts` | 新增：埋点写入 `audit_logs`（fire-and-forget） |
| `api/src/app/api/events/route.ts` | 新增：前端事件上报接口（单次 ≤ 20 条） |
| `src/utils/usageTracker.js` | 新增：前端埋点（sendBeacon 优先），登录后 `setTrackingContext` 注入用户 |
| `api/src/lib/n8n/client.ts` | `call()` 内统一埋点，覆盖全部 AI 工作流调用（成功/失败/耗时） |
| `api/src/app/api/rag/generate/route.ts` | 检索层切 RAGFlow（`RAGFLOW_ENABLED=true` 时），失败回落 PG |
| `api/src/app/api/rag/upload-knowledge/route.ts` | 保存 PG 后同步上传 RAGFlow 并触发解析；新增 `ragflow_document_id` 列 |
| `api/src/app/api/rag/knowledge/[id]/route.ts` | 删除时同步删 RAGFlow 文档（原 Qdrant 死代码移除） |
| `api/scripts/sync-usage-habits.mjs` | 新增：习惯画像 ETL |

### 4. 开启与验证

```bash
# .env.docker / 生产 env 中：
RAGFLOW_ENABLED=true
RAGFLOW_API_URL=http://<服务器IP>:9380
RAGFLOW_API_KEY=ragflow-xxxx
```

1. 绘本知识库上传一篇文档 → RAGFlow `picturebook-knowledge` 数据集出现文档且解析为 DONE
2. `/api/rag/generate` 请求日志出现 RAGFlow 检索（或回落日志）
3. 任意触发一次 AI 生成 → `audit_logs` 出现 `ai.workflow.call`
4. `cd api && node scripts/sync-usage-habits.mjs --days=30` → `usage-habits` 数据集出现组织/用户画像
5. cron：`0 3 * * * cd /path/api && node scripts/sync-usage-habits.mjs`

### 5. 埋点 action 规范

格式 `{域}.{对象}.{动作}`，新增埋点前先登记：

| action | 触发点 |
|--------|--------|
| `ai.workflow.call` / `ai.workflow.error` | n8n 统一入口（自动，全 AI 路由） |
| `rag.knowledge.upload` | 知识上传（自动） |
| `page.view` / `asset.download` / `course.copy` 等 | 前端 `trackEvent()` 按需接入 |

### 6. 数据边界与隐私

* 使用习惯画像仅存 RAGFlow `usage-habits` 数据集，**不进入 Wiki.js / Git 内容仓库**
* 画像只含行为统计（次数/偏好/时段），不落原始 prompt 全文与个人信息
* `audit_logs.details` 中 courseId/resourceId 仅为关联标识，脱敏聚合后才入画像

---

## 六、 提示词库：代码提示词外置到知识库（已实施）

> 业务系统生成内容/图片的提示词原先硬编码在 `api/src/prompts/*.ts` 及各路由内。现已支持外置到 Wiki.js 知识库：**Wiki 为权威版本，教研/运营改提示词无需发版**；代码内置模板降级为兜底快照。

### 1. 机制

```
docs/markdown/wellbeing/prompts/*.md ──Git 同步──► Wiki.js（wellbeing/prompts/*）
                                                       │ GraphQL singleByPath
业务 API: loadPromptTemplate(key)                       ▼
  ├─ WIKI_PROMPT_ENABLED=true → 读页面 → 解析 ## system / ## user 段 → {{var}} 渲染 → 5 分钟缓存
  └─ 未启用 / Wiki 不可用 / 页面缺失 → 自动回落 api/src/prompts/ 内置模板（业务不中断）
```

* 页面格式与变量说明：`docs/markdown/wellbeing/prompts/index.md`
* 模板为纯文本：条件文案（中英文输出规则等）由调用方组装为变量（如 `{{languageRule}}`）注入，模板不含逻辑
* 缓存 TTL 默认 300s（`WIKI_PROMPT_CACHE_TTL_SEC`），改完提示词最迟 5 分钟生效；Git 同步链路上 Wiki 侧约 5 分钟拉取

### 2. 本次改动

| 文件 | 改动 |
|------|------|
| `api/src/lib/prompt-library.ts` | 新增：Wiki.js GraphQL 加载器（缓存/解析/渲染/回落） |
| `docs/markdown/wellbeing/prompts/index.md` | 新增：提示词库索引与格式规范 |
| `docs/markdown/wellbeing/prompts/picture-book-activity-plan.md` | 新增：绘本活动计划模板（自代码迁移） |
| `docs/markdown/wellbeing/prompts/picture-book-design.md` | 新增：绘本逐页设计模板（自代码迁移） |
| `api/src/app/api/rag/generate/route.ts` | 两个生成分支改为知识库模板优先，代码模板兜底 |
| `.env.docker.example` / `api/.env.production.example` | 新增 `WIKI_PROMPT_*` 变量（默认关闭） |

### 3. 开启步骤

1. `sync-to-wiki.sh` 推送（新增 prompts 目录随 `docs/markdown` 一并同步），Wiki 确认 3 个页面存在
2. Wiki.js **Administration → API Access** 创建 API Key（最小权限 `read:pages`）
3. env 配置：`WIKI_PROMPT_ENABLED=true`、`WIKI_API_URL`、`WIKI_API_KEY` → 重启 backend
4. 验证：调一次 `/api/rag/generate`，日志无 `[prompt-library]` 告警即命中 Wiki 模板；在 Wiki 修改模板一处措辞 → 5 分钟后生成结果随之变化

### 4. 其余提示词迁移路线

`course-journey.ts` / `song-writing.ts` / `video.ts` 及各 `ai/generate-*` 路由内联提示词迁移方式相同：代码模板变量化搬入 `wellbeing/prompts/` 新页面 → 路由改为 `loadPromptTemplate()` 优先 + 原函数兜底 → 在索引页登记。建议按使用频率分批迁移，每批灰度验证。

### 5. 注意事项

* 兜底快照仅作灾备：启用后**只在 Wiki 维护模板**，避免两份漂移
* 提示词页面对所有 Wiki 可见角色开放编辑前，建议用 Page Rules 将 `wellbeing/prompts/*` 限制为教研/研发组
* 教学硬约束（页数/词数/禁止事项）的修改走教研评审，与知识库规范页同等对待

---

## 七、 提示词统一注册表（已实施）

> 摸排结论：提示词原散落在 6 个 LLM 直调路由、24 个 n8n 路由（10 种字段名）、前端页面与 n8n 工作流内部。现已建立统一注册表，后端能控制的提示词全部收口到一个入口，并在知识库建立全量台账。

### 1. 统一入口

```ts
import { getPrompt, getPromptPair } from '@/prompts/registry';

const text = await getPrompt('ppt.poster-b2', { scene: '...' });        // 单段（碎片）
const pair = await getPromptPair('ai.word-emojis', {}, { words });      // 双段（system/user）
```

解析顺序：**Wiki 页面（`wellbeing/prompts/<key>`，启用时）→ `builtin.ts` 内置模板 → 抛错**（未登记直接报错，倒逼走登记流程）。

### 2. 改动清单

| 文件 | 改动 |
|------|------|
| `api/src/prompts/registry.ts` | 新增：`getPrompt/getPromptPair` 唯一入口 + `PROMPT_META` 全量台账（28 个 key，标注生效位置） |
| `api/src/prompts/builtin.ts` | 新增：内置模板集中地（14 个碎片 key，自 5 个路由的内联文本收编） |
| `api/src/prompts/picture-book.ts` | 重构：抽出 `ACTIVITY_PLAN_TEMPLATE` / `PICTURE_BOOK_DESIGN_TEMPLATE` 纯模板常量，构建函数改为渲染（一份文本 builtin 与函数共用） |
| `api/src/lib/prompt-library.ts` | 扩展：整页无二级标题时作为 `main` 段（碎片模板也能走 Wiki 覆盖） |
| `ai/generate-word-emojis` | 内联 system prompt → `getPromptPair('ai.word-emojis')` |
| `ai/generate-course` | outputInstruction/overview_text 内联 → `getPrompt` |
| `ai/generate-course-overview` | TEXTLESS 常量与 buildOutputInstruction → `getPrompt`（原函数删除） |
| `ai/generate-ppt-asset` | B2 海报/B3 词卡模板 → `getPrompt`（构建函数改 async） |
| `ai/generate-scene` | 两条 negative_prompt → `getPrompt` |
| `rag/generate` | resolve* 统一走 `getPromptPair`（原直连 loadPromptTemplate 的路径合并） |

### 3. 全量台账

提示词修改入口见知识库页 `wellbeing/prompts`（四分区）：

1. **已收编**（14 key）：Wiki 覆盖或改 `builtin.ts`
2. **prompts/ 模块**（5 key）：改 `api/src/prompts/` 对应文件
3. **前端组装**（4 key）：改前端页面（后端只透传）
4. **n8n 工作流内部**（8 key）：登录 n8n 改工作流节点

### 4. 新增提示词流程（固化）

文本入 `builtin.ts` → `PROMPT_META` 登记 → 路由只 import registry（禁止内联文本）→ 台账页登记。未注册的 key 调用即抛错，编译期之外再设一道运行时防线。

### 5. 迁移效果

* 后端内联提示词清零：5 个路由的内联碎片全部收编，改提示词不再需要全文检索代码
* `grep` 可验证：`src/app/api` 下不再出现提示词长文本（仅剩 registry/builtin/模块三处集中地）
* Wiki 覆盖粒度到单个 key：教研可在知识库改 B3 词卡模板、负面提示词等，5 分钟生效

## 八、 提示词统一二期：n8n 工作流 + 前端收编（已实施）

> 一期收口了后端内联提示词，二期消灭剩余两大死角：**n8n 工作流节点内部的 8 组 LLM 提示词**（改提示词要登 n8n 后台）与**前端页面的硬编码提示词常量**。至此全系统提示词单一入口完成。

### 1. n8n 侧改造（9 个工作流、16 个模板 key）

**数据流**：后端路由 `getRawTemplate(key)` 取**原始模板**（不渲染）→ 随 payload 下发 `promptTemplate` 字段 → n8n 参数组装 Code 节点注入的 `__renderPrompt` 用自身参数渲染 `{{var}}` → LLM 节点统一改读 `={{ $json.system_prompt }}`。

设计决策：
- **模板原文下发而非渲染后文本**——时长预算、插入上下文等参数计算仍留在 n8n Code 节点（不重复实现），渲染占位符逻辑两边共用同一语法
- **模板缺失即抛错**——Code 节点检测不到 `promptTemplate` 直接报「请先部署最新 api」，倒逼部署顺序：**先 api 后 n8n 导入**
- **表达式语法转换**——`{{ $json.xxx }}` → `{{xxx}}`、`{{ $json.a.b }}` → `{{a__b}}`（双下划线表嵌套）、3 个特殊表达式专项处理（`slideIndex + 1` → `slideIndexPlus1`、`JSON.stringify(slidePlan)` → `slidePlanJson` 等）

四类工作流结构分别处理：6 个标准链（Webhook→参数组装→LLM）、PPT 双链（Plan 链 + Slide 链 map 逐页渲染）、ai-prompt-optimize（7 个 task_type 模板映射整块移除）、ai-prompt-processing（静态 Agent 提示词）。

| 产物 | 说明 |
|------|------|
| `api/scripts/n8n-prompt-inventory.mjs` | 盘点工作流节点结构（迁移前摸底） |
| `api/scripts/n8n-prompt-extract.mjs` | 提取全部提示词供人工审查（`out-n8n-prompts.json`） |
| `api/scripts/n8n-prompt-inject.mjs` | 一次性迁移：提取→转换→改造工作流→生成模板快照（**仅可运行一次**） |
| `api/src/prompts/n8n-templates.json` | 16 个模板的兜底快照（勿手改，Wiki 可覆盖） |
| `n8n/` 下 9 个工作流 JSON | 已改造：Code 节点含渲染逻辑，LLM 节点不再内联提示词 |

迁移后校验：全部 16 个 Code 节点 `new Function` 语法通过、注入代码引用变量作用域核验、模板无 `$json` 表达式残留。

### 2. 后端接入

| 文件 | 改动 |
|------|------|
| `api/src/prompts/registry.ts` | 新增 `getRawTemplate(key)`（原文下发专用）+ `PROMPT_META` 更新为 33 个已收编 key |
| `api/src/prompts/builtin.ts` | 加载 `n8n-templates.json` 展开合入 + 收编 4 个前端 key |
| 10 个 n8n 调用路由 | payload 附 `promptTemplate`（PPT 双字段 `promptTemplate`/`promptTemplateSlide`；optimize 按 task_type 取模板、未登记回落 general） |

### 3. 前端接入

| 文件 | 改动 |
|------|------|
| `api/src/app/api/prompts/[key]/route.ts` | 新增 `GET /api/prompts/<key>`：返回模板原文，`?vars=JSON` 可选渲染 |
| `src/services/promptLibrary.js` | 新增前端服务：`getPromptTemplate(key, 本地兜底)` + `renderPromptTemplate`（与后端同语法） |
| `PictureBookStudioPage.jsx` | 视觉风格/负面提示词改 state 拉取（useEffect 加载，本地常量仅兜底） |
| `aiAssetService.js` | ComfyUI 负面提示词改拉取 |
| `videoStoryboardService.js` | 人物参考图模板改拉取（顺带删除恒 404 的 `/api/ai/get-character-prompt` 死代码） |
| `src/constants/aiOptions.js` | 新增：VOICE/EMOTION/SPEED/AUDIO_STYLES 去重收敛（原 5 文件 10 份副本，取超集：14 音色、8 情感） |

### 4. 验证与部署顺序（v2 共存方案）

n8n 实例无测试/正式区分，为避免「导入即覆盖线上」，9 个改造后的工作流调用名已统一加 `-v2` 后缀（webhook path、webhookId、工作流名三处），与 v1 老流程在同一实例共存：

| 工作流 | v2 调用名（webhook path） |
|---|---|
| 根据课程参数生成创意任务名称 | `course-idea-generator-v2` |
| 润色优化已有的任务名称 | `course-content-polisher-v2` |
| 课程概览调整建议生成 | `course-overview-adjustment-tips-generator-v2` |
| 单步骤课程生成 | `course-step-generator-v2` |
| 单环节重新生成 | `course-step-regenerator-v2` |
| 单阶段课程重新生成 | `course-phase-regenerator-v2` |
| PPT英文内容与布局生成 | `ppt-content-generator-v2` |
| ai-prompt-optimize | `ai-prompt-optimize-v2` |
| ai-prompt-processing | `ai-prompt-processing-v2` |

后端调用点（10 个路由文件）已同步指向 v2，脚本见 `api/scripts/n8n-rename-v2.mjs`。

```
部署步骤：
1. n8n 导入 9 个 v2 工作流（n8n/ 目录，名称带 [v2] 标识）并激活
   ← 先导入不影响线上：v1 老流程仍在服务旧版 api 的调用
2. api 部署（含 registry/builtin/n8n-templates.json + v2 调用名）
   ← 部署后流量即切换到 v2 新流程
3. 前端部署
4. 冒烟验证：POST /api/ai/generate-course-idea 等，观察 n8n 执行
   「[v2] 工作流」无「缺少 promptTemplate」报错
5. 稳定后清理：删除 n8n 中 9 个 v1 老流程（不带 [v2] 标识的同名工作流）

回滚方式：api 回退上一版本即回到 v1 老流程，v2 工作流可保留待修复。
```

编译验证：`api` 下 `tsc --noEmit` 通过；前端 eslint 改动文件零新增错误。